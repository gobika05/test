#include<stdio.h>
int main()
{
    float x,y=2,sum;
    printf("enter any number:");
    scanf("%f",&x);
    sum=x+y;
    printf("the given  number is added with 2 and the new number is:%.2f",sum);
    return 0;

}